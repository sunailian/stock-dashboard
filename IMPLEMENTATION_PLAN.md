# 结构化交易决策升级方案

## 目标

把当前基于盈亏比例的单句建议，升级为一套可解释、可审计、可复盘的辅助决策流程。系统只提供研究和风险提示，不自动连接券商下单。

## 第一阶段：结构化决策（本次执行）

- 点击持仓后，生成五级评级：买入、加仓、持有、减仓、卖出。
- 输出置信度、多头证据、空头证据、执行摘要、建议仓位、入场价、止损价、目标价、持有周期和逻辑失效条件。
- 将当前仓位、板块占比、同一公司跨市场敞口纳入硬风控上下文。
- BABA 与 9988.HK 合并计算阿里巴巴经济敞口；半导体板块统一计算集中度。
- DeepSeek 不可用时自动回退到确定性规则，不让详情页失效。
- 浏览器本地保存最近 100 条决策；再次分析时显示上次建议及价格变化。
- 分析结果缓存 6 小时，价格变化超过 2% 或用户主动刷新时重新分析，避免每次点击重复消耗模型调用。

## 第二阶段：数据落地与复盘

- [已完成第一版] 决策协议 v2：保存模型原始评级、最终评级、一致性问题、调整动作、变化原因和新证据。
- [已完成第一版] 浏览器保存每日价格快照，并在建议后 5、20、60 个交易日附近存在快照时计算收益；美股同时记录相对 S&P 500 的超额收益。缺失对应日期快照时不使用更晚价格冒充历史结果。
- [已完成第一版] 将已到期方向命中率反馈给服务端校准置信度；少于 10 个样本时置信度封顶 70%。
- [已完成第一版] 确定性一致性校验：拦截评级与文字冲突、错误价格关系、无新证据的方向反转和集中度违规。
- [待基础设施] 将浏览器决策日志迁移到 OSS、TableStore 或数据库。阿里云 FC 临时文件系统不作为长期记录来源。
- [待数据增强] 增加平均超额收益、最大有利/不利波动、评级转移矩阵，并将逻辑失效条件转化为可监控告警。

## 第三阶段：研究数据增强

- 接入真实历史 K 线和技术指标，替换当前模型示意曲线。
- 接入财报、估值、新闻和宏观数据，并记录每项数据的来源与时间。
- 分别生成基本面、技术面、新闻与情绪报告，再进行多空证据汇总。
- 分析按每日、财报或重大事件触发；实时价格仍独立按 30 秒刷新。

## 第四阶段：组合级决策

- 按真实经济敞口合并跨市场同一公司和 ETF 成分重叠。
- 增加相关性、行业集中度、预期回撤、现金缓冲和单笔风险预算。
- 使用硬规则审批 AI 建议，任何加仓建议都必须满足仓位和风险预算约束。

## 第一阶段接口

`POST /analysis`

输入：标的、原币种价格、成本、数量、收益率、行业、持仓权重、行业权重、同公司敞口和年化目标。

输出：评级、置信度、执行摘要、多空证据、仓位建议、入场/止损/目标价、持有周期、失效条件、风险标记、数据范围和生成时间。

## 实时账户数据（已完成）

- 新增 `GET /account`，服务端使用 LongPort OpenAPI 的 `/v1/asset/stock`、`/v1/asset/account` 和 `/v1/asset/exchange_rates` 获取实时持仓数量、可用数量、券商成本、现金、购买力、净资产和账户汇率。
- FC 通过 `LONGBRIDGE_APP_KEY`、`LONGBRIDGE_APP_SECRET`、`LONGBRIDGE_ACCESS_TOKEN`（兼容 `LONGPORT_*` 名称）读取凭据；任何密钥不得进入 HTML 或 Git。
- 实时账户与分析接口要求服务端会话令牌。FC 还必须配置 `DASHBOARD_PASSWORD_HASH`（访问密码的 SHA-256）和随机的 `DASHBOARD_SESSION_SECRET`；HTML 不再包含可离线比对的密码哈希。
- 前端已删除写死的持仓数量、成本、现金、购买力、净资产和默认价格。账户接口失败或缺少任一持仓价格/币种汇率时，明确标记 `ACCOUNT BLOCKED`，不使用旧静态持仓兜底。
- 账户快照每30秒刷新，FC 热实例仅缓存15秒。持仓分析请求会再次强制读取实时账户，并以服务端数量、成本、价格和仓位权重覆盖前端输入；已卖出的股票返回409，不再生成持仓建议。
- 分析缓存版本升级，数量、成本或组合权重变化会使旧建议失效。负成本仓位保留券商原始成本显示，但不再用普通收益率公式污染组合收益率。
- 持仓占比、板块占比和健康度集中度统一改用实时市值；历史资产走势在接入可验证净值数据前停止显示随机模拟曲线。

## 持仓建议与组合外发现

- 持仓表格继续使用 `/analysis` 生成持有、加仓、减仓等操作建议；“潜力个股发现”不再从现有持仓中排序。
- 新增 `POST /recommendations`：严格排除已持仓股票及同公司跨市场标的，从预先审核的美股/港股研究范围中读取一年真实日线，按20/60/120日动量、50/200日趋势、20日波动、120日回撤和行业互补性生成0—100分量化排名。
- 候选必须同时通过“价格在200日均线上方、60日动量为正、回撤与波动不过线”的准入规则；数据不足或行情失败时显示不可用，不使用持仓或虚构价格兜底。
- 推荐卡片展示量化评分、原币种观察价、建议仓位上限和组合互补理由；点击后展示真实120日价格曲线、建议入场/止损/目标价、多空证据、执行步骤与失效条件。
- 日线市场数据在 FC 热实例缓存6小时，浏览器候选结果也缓存6小时；用户可在候选详情主动刷新。20%年化是组合目标，不被解释成个股收益保证。
- 美股和港股都从持仓表格点击进入同一个 `/analysis` 决策流程，避免页面同时出现多个相互冲突的建议。
- 行情每 30 秒刷新；建议缓存有效期为 2 小时。价格变化达到 1% 或组合权重签名变化时，打开的个股明细会自动重新分析；用户也可以手动强制刷新。
- 相同价格、成本和组合敞口下保持相同操作方向属于稳定性要求。模型若想改变方向，必须同时满足价格/敞口变化阈值并提供可验证的新证据。
- 明细统一展示：建议操作、入场价、目标价、止损价、目标持仓占比、上涨空间、收益风险比、建议理由、风险理由、执行步骤和重新分析触发条件。
